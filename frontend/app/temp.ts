import React from 'react';
import { ResponsiveContainer } from '../components/ResponsiveContainer';
import HomeScreen from '../app/(tabs)/index'; // Import component logic from original files
import { View, Text } from 'react-native';

// Re-exporting screens wrapped in ResponsiveContainer
// Actually, it's better to wrap inside the screens themselves to avoid duplication
// But for now, I'll update the screens to use the component directly.
